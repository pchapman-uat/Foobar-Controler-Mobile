import { StyleSheet } from "react-native";

export const MainStyle = StyleSheet.create({
  view: {
      flex: 1,
  },
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },

  statusItem: {
    textAlign: 'center'
  },
  textInput: {
    borderWidth: 1,
    margin: 5,
  }
}); 